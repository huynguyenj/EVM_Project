export enum ContractPaidType {
  FULL = 'FULL',
  DEBT = 'DEBT',
}
export enum ContractType {
  AT_STORE = 'AT_STORE',
  ORDER = 'ORDER',
  PRE_ORDER = 'PRE_ORDER',
}
export enum ContractStatus {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  PROCESSING = 'PROCESSING',
  DELIVERED = 'DELIVERED',
  COMPLETED = 'COMPLETED',
}
